# Deployment Report — SRP Smart Recruit v1.0.0

**Environment:** Hetzner production `/opt/srp-smartrecruit-auth`  
**URL:** https://recruit.srpailabs.com  
**Workflow:** Deploy Frontend (Next.js Auth) — push to `main`  
**Successful run:** 30409020848 (2026-07-28T23:45:59Z → 23:55:00Z UTC)  
**Failed attempt (resolved):** 30408522666 — backup aborted on optional Supabase upload; fixed in `8dcbd65`

## Order executed

1. Code freeze gates (CI `tsc` / `lint` / `build`) — PASS  
2. `git reset --hard` to deploy SHA; `git clean` excluding `uploads` / `.env`  
3. Pre-deploy `srp-backup` — local Auth + ATS dumps; Backup COMPLETE  
4. Write `.env` from `PRODUCTION_ENV`  
5. Additive migrations v10–v13 + tracked v0/v14–v32  
6. `docker compose build app` (old container served during build)  
7. `force-recreate app` only (DB untouched)  
8. Localhost health 200 (attempt 2)  
9. Nginx SoT update + reload  
10. HTTPS E2E — PASS  

## Impact

| Asset | Outcome |
|-------|---------|
| Tenant / auth users | Preserved (10 before = 10 after) |
| Auth DB | Migrated additively; not reset |
| Uploads | Not wiped (`git clean -e uploads`) |
| Downtime | Brief during app recreate (~minutes of build with prior image still serving) |

## Rollback point

`/opt/backups/srp-smartrecruit/2026-07-28_234750/auth_database.dump` (plus ATS dump from same run)

## Rollback procedure

1. `maintenance_mode` if needed  
2. Redeploy prior known-good commit  
3. Leave migrations in place  
4. Restore dump only for corruption (Owner approval)
